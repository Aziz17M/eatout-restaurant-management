<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}EatOut{% endblock %} - Dine With Ease</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🍽️</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% if app.user %}
        <nav class="navbar">
            <div class="container">
                <a href="{{ is_granted('ROLE_ADMIN') ? path('app_admin_dashboard') : path('app_reservation_index') }}" class="navbar-brand">
                    EatOut
                </a>
                <div class="navbar-menu">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_admin_dashboard') }}">Dashboard</a>
                    {% endif %}
                    <a href="{{ path('app_restaurant_index') }}">Restaurants</a>
                    <a href="{{ path('app_dish_index') }}">Menu</a>
                    <a href="{{ path('app_reservation_index') }}">My Reservations</a>
                    <div class="user-info">
                        <a href="{{ path('app_profile') }}">👤 {{ app.user.name ?? app.user.email }}</a>
                        <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                </div>
            </div>
        </nav>
        {% endif %}
        {% block body %}{% endblock %}
    </body>
</html>
